export default function UserMenu() {
  const handleLogOut = () => {};
  return (
    <div>
      <p>Welcome</p>
      <button type="button" onClick={handleLogOut}>
        Logout
      </button>
    </div>
  );
}
